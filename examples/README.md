Must launch gamepad_tutorial using `cargo run --example gamepad_tutorial --features winit`

If your IDE disables areas marked with `#[cfg(feature=winit)]` edit `Cargo.toml` and uncomment `default = ["winit"]` in `[features]`
